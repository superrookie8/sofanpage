[build]
  command = "pnpm build"
  publish = ".next"

[build.environment]
  # Netlify는 기본값이 Node 18이라 Agent Runner 경고가 발생한다.
  # NODE_VERSION이 .nvmrc보다 우선하므로 명시적으로 고정한다.
  NODE_VERSION = "24"

[[plugins]]
  package = "@netlify/plugin-nextjs"
