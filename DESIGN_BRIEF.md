# SUPER SOHEE — 리디자인 브리프

> 농구선수 이소희(BNK 썸 #6) 팬페이지 프론트엔드 리디자인 구상안
> 작성일: 2026-09-01 / 대상: Claude Design
> 스택: Next.js 15 App Router + Tailwind CSS 3 + styled-components(일부) + framer-motion

---

## 0. 이 문서 사용법

Claude Design에는 아래 순서로 전달하는 걸 권장합니다.

1. **§1 현황 진단** + **§2 디자인 방향** — 무엇을 왜 바꾸는지
2. **§3 디자인 토큰** — 색/타입/스페이싱의 단일 소스
3. **§4 컴포넌트 시스템** — 재사용 단위
4. **§5 페이지별 리디자인** — 화면별 구체 지시
5. **§8 프롬프트 스니펫** — 그대로 복붙 가능한 요청문

---

## 1. 현황 진단

### 1.1 사이트 구성

| 영역 | 경로 | 성격 |
|---|---|---|
| 홈 | `/`, `/home` | 배너 + 프로필 + 시즌 스탯 테이블 |
| 뉴스 | `/news` | 크롤링 기사(최신 / 점프볼 / 루키) |
| 이벤트 | `/events`, `/events/5th`, `/events/6th` | 팬 이벤트 아카이브, 사진 갤러리 |
| 아케이드 | `/arcade` | Unity WebGL 미니게임 + 랭킹 |
| 경기 스케줄 | `/schedule` | 캘린더 + 카카오맵 + 경기 정보 모달 |
| 직관 일기 | `/diary/*` | 좌석·날씨·승패 태그가 붙은 관람 기록 CRUD |
| 방명록 | `/guestbooks/*` | 사진/텍스트 탭 게시판 |
| 계정 | `/login`, `/signup`, `/mypage` | NextAuth 기반 |

### 1.2 발견된 문제

**A. 배경 레이어 충돌 (가장 시급)**
`layout.tsx`가 `<Background />`로 `soheeposter34.png`를 전 페이지에 `position:fixed`로 깔고, 각 페이지는 그 위에 `bg-white bg-opacity-75` 또는 `bg-black bg-opacity-75`를 덮는 구조입니다.
- 배경 사진이 흐릿하게 비쳐 본문 텍스트 대비가 떨어짐 (`text-gray-500` on 75% white → WCAG AA 미달 구간 발생)
- 페이지마다 오버레이 색이 달라 (홈=화이트, 이벤트=블랙, 스케줄=`bg-gray-100/80`) 사이트가 한 몸으로 안 읽힘
- 이벤트 상세 페이지는 아예 `document.body.classList.add("bg-white")`로 배경을 끄는 예외 처리가 들어가 있음

**B. 디자인 토큰 부재**
`bg-white` 87회, `text-white` 85회, `text-gray-500` 75회, `bg-red-500` 56회, `bg-red-600` 43회 — 전부 하드코딩. 레드 톤이 500/600/300/200으로 근거 없이 섞여 있어 브랜드 컬러가 하나로 안 읽힙니다.

**C. 타이포그래피 위계 없음**
`globals.css`의 `* { font-family: ... !important }`가 모든 요소에 단일 웨이트 폰트를 강제합니다. 결과적으로 제목·본문·캡션·숫자가 전부 같은 굵기·같은 톤이라 "어디를 먼저 봐야 하는지"가 없습니다. 특히 스탯 테이블 숫자에 tabular figure가 안 걸려 자릿수가 흔들립니다.

**D. 브레이크포인트가 Tailwind 관례와 반대**
```ts
screens: { sm: {min:320, max:500}, md: {min:501, max:950}, lg: {min:951} }
```
min-width 캐스케이드가 아니라 배타적 구간이라, 코드에 `sm:bg-white md:hidden lg:flex`가 뒤섞이고 `sm: h-[200px]`처럼 오타(콜론 뒤 공백)로 죽은 클래스도 존재합니다.

**E. 헤더에 브랜드가 없음**
로고/워드마크 없이 링크만 우측 정렬. 데스크톱은 빨강 배경 + 흰 글씨, 모바일은 흰 배경 + 빨강 글씨로 정체성이 반전됩니다. 활성 메뉴 표시가 `text-black`이라 눈에 안 띕니다.

**F. 상태 화면 미설계**
로딩은 `Loading...`, `뉴스기사를 가져오고 있습니다...`, 에러는 `Error: {message}` 생문자열. 빈 상태(일기 0건, 방명록 0건) 디자인 없음.

**G. 정보 밀도 문제**
- 프로필: `Name : ...` `Team : ...` 좌측 정렬 텍스트 나열 (7줄)
- 스탯: 14~15열 테이블을 모바일에서 가로 스크롤 — 핵심 지표(PPG/APG/3P%)가 안 보임

---

## 2. 디자인 방향

### 2.1 컨셉 후보

**A. 매치 프로그램 (Match Program)** — 권장
경기장에서 나눠주는 프로그램북 / 스포츠 매거진의 언어. 굵은 타이포, 큰 숫자, 명확한 그리드, 여백. 레드는 액센트로만.
- 장점: 스탯·스케줄·뉴스라는 사이트의 뼈대와 가장 잘 맞음. 구현 난도 낮음.
- 톤: 정돈됨, 신뢰감, "선수 공식 페이지" 같은 인상

**B. 팬 스크랩북 (Fan Scrapbook)**
폴라로이드, 티켓 스텁, 손글씨 라벨. 따뜻하고 개인적.
- 장점: 직관 일기 / 방명록 / 이벤트 사진과 궁합이 좋음
- 단점: 스탯 테이블·뉴스에는 안 어울림. 전면 적용 시 산만해짐

**C. 다크 아레나 (Dark Arena)**
짙은 배경 + 네온 레드, 사진 몰입형.
- 장점: 아케이드/이벤트 페이지에 강함
- 단점: 텍스트 많은 뉴스/일기에서 피로. 다크 팔레트를 새로 다 짜야 함

### 2.2 최종 제안: **A를 기본 뼈대로, 감성 영역에만 B의 온기를 주입**

```
┌─────────────────────────────────────────────────┐
│  기본 시스템 = 매치 프로그램 (A)                │
│  · 홈 / 뉴스 / 스케줄 / 프로필 / 스탯 / 계정    │
│                                                 │
│  액센트 존 = 스크랩북 (B)                       │
│  · 직관 일기 / 방명록 / 이벤트 갤러리           │
│    → 카드에 살짝 기울인 각도, 티켓 노치,        │
│      사진 테두리, 태그 칩                       │
│                                                 │
│  전면 다크 = (C)                                │
│  · 아케이드만. 게임 캔버스 몰입용               │
└─────────────────────────────────────────────────┘
```

세 영역은 **같은 토큰(§3)을 공유**하되 밀도와 장식만 다릅니다. 폰트도 색도 새로 만들지 않습니다.

### 2.3 디자인 원칙 5가지

1. **사진은 콘텐츠 안에서만** — 전역 고정 배경 포스터를 걷어내고, 홈 히어로 등 의도된 자리에만 큰 사진을 씁니다. 본문은 항상 불투명한 서피스 위에.
2. **레드는 액센트, 배경 아님** — 브랜드 레드는 강조·활성·CTA에만. 대형 레드 면적은 히어로와 헤더 바 정도로 제한.
3. **숫자를 주인공으로** — 스탯은 표가 아니라 카드/차트가 1차, 표는 2차(펼치기).
4. **모바일이 기본 설계** — 주 사용층이 모바일 팬입니다. 데스크톱은 모바일 레이아웃의 확장.
5. **모든 상태를 디자인** — 로딩(스켈레톤) / 빈 상태(일러스트+행동 유도) / 에러(복구 버튼) 3종 세트 필수.

---

## 3. 디자인 토큰

### 3.1 컬러

Tailwind 커스텀 컬러로 정의하고 CSS 변수로 노출합니다. 다크 모드는 1차 범위에서 제외(아케이드만 하드코딩 다크).

```
브랜드
  brand-50   #FFF1F2   아주 옅은 레드 배경 (선택 상태, 뱃지 바닥)
  brand-100  #FFE1E3   호버 배경
  brand-200  #FFC7CB   보더 라이트
  brand-400  #F4576B   호버 상태 레드
  brand-500  #E11D33   ★ 프라이머리 (현 red-500/600 통합점)
  brand-600  #C1152A   눌림/액티브
  brand-700  #8F0F1F   텍스트용 진한 레드 (흰 배경 대비 AA 통과)

중립 (따뜻한 회색 — 코트 바닥 느낌)
  ink-900    #17151A   최상위 제목
  ink-700    #3A363F   본문
  ink-500    #6B6570   보조 텍스트  ← 현 text-gray-500 대체
  ink-300    #ADA7B2   비활성/플레이스홀더
  ink-200    #DDD9E0   구분선
  ink-100    #EFECF1   서피스 대체
  ink-50     #F8F6F9   페이지 배경
  white      #FFFFFF   카드 서피스

시맨틱
  win        #0E9F6E   승리 태그
  lose       #6B6570   패배 태그 (레드 금지 — 브랜드와 충돌)
  info       #2563EB   링크/정보
  warn       #D97706   경고
```

**대비 규칙**
- 본문 텍스트는 `ink-700` 이상만 사용. `ink-500`은 14px 이상 보조 정보에만.
- 반투명 배경 위 텍스트 금지. 오버레이가 필요하면 불투명 서피스 + 사진은 그 옆/뒤에 배치.
- 레드 배경 위 텍스트는 반드시 흰색, 최소 15px/Bold.

### 3.2 타이포그래피

**폰트 전략을 바꿉니다.** GmarketSansMedium 단일 웨이트로는 위계를 만들 수 없습니다.

**결정: Pretendard Variable 전면 전환** (2026-09-01 확정)

```
표제 / 본문 / 숫자 : Pretendard Variable (100~900 가변)
숫자 전용          : Pretendard + font-variant-numeric: tabular-nums
영문 디스플레이(선택): 스포츠 저지 느낌의 콘덴스드 산세리프
                     (예: Archivo Expanded / Anton)
                     — "SUPER SOHEE", "#6" 같은 워드마크에만
```

기존 GmarketSansMedium은 단일 웨이트라 위계를 만들 수 없어 교체합니다.
브랜드 인상 변화는 감수하되, 워드마크에서 디스플레이 서체로 개성을 확보합니다.

`globals.css`의 `* { font-family: ... !important }`는 **삭제**하고 `body`에만 지정합니다.

**타입 스케일 (모바일 → 데스크톱)**

| 토큰 | 크기 | 웨이트 | 용도 |
|---|---|---|---|
| `display` | 32 → 48px | 800 | 히어로 이름, 큰 숫자 |
| `h1` | 24 → 32px | 700 | 페이지 제목 |
| `h2` | 19 → 22px | 700 | 섹션 제목 |
| `h3` | 16 → 18px | 600 | 카드 제목 |
| `body` | 15 → 16px | 400 | 본문 |
| `sm` | 13 → 14px | 400 | 보조 |
| `caption` | 12px | 500 | 라벨, 타임스탬프 |
| `stat` | 28 → 36px | 800 / tabular | 스탯 수치 |
| `stat-label` | 11px | 600 / +0.06em | 스탯 라벨 (PPG 등, 대문자) |

행간: 제목 1.25 / 본문 1.65 / 캡션 1.4. 한글은 `word-break: keep-all` 유지.

### 3.3 스페이싱 · 형태

```
간격 스케일: 4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 (8pt 기반)
컨테이너   : 모바일 좌우 padding 16px, 최대폭 1120px (현 max-w-6xl 유지)
라운드     : sm 6px (칩·인풋) / md 12px (카드) / lg 20px (히어로·모달) / full (아바타·필터칩)
그림자     : 
  soft   0 1px 2px rgba(23,21,26,.06), 0 2px 8px rgba(23,21,26,.04)
  raised 0 4px 16px rgba(23,21,26,.08)
  modal  0 16px 48px rgba(23,21,26,.18)
보더      : 1px solid ink-200 (그림자보다 보더 우선 — 사진 위에서도 형태가 유지됨)
```

### 3.4 브레이크포인트 (수정 필요)

현재의 배타적 구간을 표준 min-width 캐스케이드로 되돌립니다.

```ts
screens: {
  sm: "480px",   // 큰 폰
  md: "768px",   // 태블릿
  lg: "1024px",  // 데스크톱
  xl: "1280px",
}
```
> ⚠️ 이 변경은 기존 `sm:` 클래스 의미를 뒤집으므로 **마이그레이션 작업이 동반**됩니다. §7 로드맵 Phase 0 참고.

---

## 4. 컴포넌트 시스템

### 4.1 신규/정비 대상

| 컴포넌트 | 설명 | 현재 상태 |
|---|---|---|
| `PageHeader` | 페이지 제목 + 부제 + 우측 액션 슬롯 | 없음 (페이지마다 다른 레드 바) |
| `Surface` / `Card` | 불투명 카드, `soft` 그림자 + `ink-200` 보더 | `bg-white bg-opacity-75` 산재 |
| `StatCard` | 큰 숫자 + 라벨 + 전 시즌 대비 델타 | 없음 |
| `StatTable` | tabular-nums, 첫 열 sticky, 가로 스크롤 힌트(페이드) | 있으나 미정비 |
| `SeasonPicker` | 시즌 선택 세그먼트/드롭다운 | 기본 `<select>` |
| `SegmentedTabs` | Average/Total, 사진/글 등 2~3분기 탭 | 페이지마다 다른 버튼 구현 |
| `NewsCard` | 썸네일 + 매체 + 제목 2줄 클램프 + 날짜 | 섹션별 제각각 |
| `GameCard` | 홈/원정 뱃지, 상대 팀, 시각, 경기장 | 캘린더 셀 내부에만 |
| `DiaryCard` | 사진 + 좌석/날씨/승패 칩 + 날짜 (스크랩북 톤) | 있으나 미정비 |
| `Chip` / `Tag` | 필터·속성 표시. variant: default/win/lose/selected | 없음 |
| `Button` | primary / secondary / ghost / danger × sm·md·lg | 인라인 클래스 |
| `EmptyState` | 아이콘 + 한 줄 설명 + CTA | 없음 |
| `Skeleton` | 카드/테이블/리스트 형태별 | `Loading...` 텍스트 |
| `ErrorState` | 메시지 + 다시 시도 버튼 | `Error: ...` 생문자 |
| `Modal` | 공통 셸. 현 `.ReactModal__Content` 하드코딩 CSS 대체 | ⚠️ 모바일에서 `top: 200% !important` 하드코딩 버그 |
| `BottomNav` | 모바일 하단 탭 (신규 제안) | 없음 |

### 4.2 헤더 리디자인

```
데스크톱 (≥1024px)
┌───────────────────────────────────────────────────────────────┐
│  [SS] SUPER SOHEE   홈  뉴스  이벤트  아케이드  스케줄  일기   [로그인] │
└───────────────────────────────────────────────────────────────┘
 · 배경 white, 하단 1px ink-200 보더, 스크롤 시 soft 그림자
 · 활성 링크: ink-900 + 하단 2px brand-500 언더라인
 · 워드마크 좌측 고정 — 어느 페이지에서도 정체성 유지
 · 스크롤 다운 시 축소(64→52px), 업 시 복귀

모바일 (<1024px)
┌───────────────────────────────┐
│  [SS] SUPER SOHEE        [☰]  │  ← 상단: 브랜드 + 계정/메뉴
└───────────────────────────────┘
              ⋮ 본문 ⋮
┌───────────────────────────────┐
│  홈   뉴스   스케줄   일기   나  │  ← 하단 고정 탭 (아이콘+라벨)
└───────────────────────────────┘
 · 5개 초과 항목(이벤트/아케이드/방명록)은 "나" 탭 또는 ☰ 안으로
 · 하단 탭은 safe-area-inset-bottom 반영
```

### 4.3 배경 처리 (핵심 변경)

`<Background />`의 전역 고정 포스터를 **제거**하고 다음으로 대체:

```
1. 페이지 배경  : 단색 ink-50
2. 홈 히어로    : 포스터/배너를 상단 히어로 블록 안에 배치 (min-h 46vh),
                  하단으로 갈수록 ink-50로 수렴하는 그라디언트 마스크
3. 섹션 구분    : 흰 카드 vs ink-50 배경의 명도 차로만 (반투명 없음)
4. 사진 강조가 필요한 페이지(이벤트/아케이드)만 로컬 배경 허용
```

이렇게 하면 이벤트 페이지의 `document.body.classList.add("bg-white")` 예외 처리와 `.background { background-attachment: fixed }` 트릭이 전부 불필요해집니다.

---

## 5. 페이지별 리디자인

### 5.1 홈 `/home`

현재: 배너 이미지 → 레드 바 "프로필" → 좌측 정렬 텍스트 7줄 → 레드 바 "기록" → 14열 테이블

**제안 레이아웃 (모바일 기준)**

```
┌─────────────────────────────────┐
│                                 │
│      [ 히어로 사진 ]            │  ← 인물 사진, 하단 그라디언트
│   LEE SOHEE            ┌────┐   │
│   이소희 · BNK 썸      │ 6  │   │  ← 등번호를 큰 그래픽으로
│   Guard · 170cm        └────┘   │
│                                 │
├─────────────────────────────────┤
│  2025-26 시즌                   │
│  ┌───────┐ ┌───────┐ ┌───────┐  │
│  │ 12.4  │ │  3.8  │ │ 36.2% │  │  ← StatCard 3~4개
│  │  PPG  │ │  APG  │ │  3P%  │  │     핵심 지표만
│  └───────┘ └───────┘ └───────┘  │
│         [ 전체 기록 보기 ▾ ]     │  ← 펼치면 기존 풀 테이블
├─────────────────────────────────┤
│  다음 경기                       │
│  ┌─────────────────────────────┐│
│  │ 10.14 (화) 19:00            ││  ← GameCard, /schedule로 딥링크
│  │ vs 우리은행 · 사직실내체육관 ││
│  └─────────────────────────────┘│
├─────────────────────────────────┤
│  최신 소식               더보기 →│
│  ┌─────────────────────────────┐│
│  │ [썸네일] 기사 제목 두 줄까지 ││  ← NewsCard 3개
│  └─────────────────────────────┘│
├─────────────────────────────────┤
│  팬들의 직관 일기        더보기 →│
│  [사진][사진][사진]  ← 가로 스크롤│
└─────────────────────────────────┘
```

**핵심 변경**
- 프로필을 `Name : ...` 나열이 아니라 **히어로 안의 메타 라인 + 등번호 그래픽**으로 흡수
- 스탯은 StatCard 3~4장이 1차, 풀 테이블은 접힘(disclosure)
- 홈이 **허브** 역할을 하도록 다음 경기 / 뉴스 / 일기 요약을 끌어올림 (현재 홈에서 다른 페이지로 갈 동선이 헤더뿐)
- 데스크톱은 히어로 좌측 사진 / 우측 프로필+스탯 2컬럼

### 5.2 뉴스 `/news`

현재: 흰 반투명 판 위 최신기사 1개 + 점프볼/루키 2컬럼 페이지네이션

**제안**
- 상단 `PageHeader`("뉴스") + 출처 필터칩 `[전체] [점프볼] [루키]`
- 최신 기사 1건은 **와이드 피처 카드** (썸네일 16:9, 제목 h2, 매체/날짜 캡션)
- 나머지는 **단일 스트림 리스트**로 통합. 2컬럼 병렬 + 각각 페이지네이션은 스캔 비용이 큽니다.
  - 모바일: 1열 / 데스크톱: 2열 그리드
  - 페이지네이션 대신 "더 보기" 버튼 또는 무한 스크롤
- 카드 요소: 썸네일(없으면 브랜드 플레이스홀더) / 출처 칩 / 제목 2줄 클램프 / 상대 시각("3시간 전")
- 로딩은 카드 모양 스켈레톤 5개

### 5.3 경기 스케줄 `/schedule`

현재: 캘린더 + 카카오맵 + 모달

**제안**
- 상단에 **다음 경기 하이라이트 카드** (D-day, 상대, 시각, 홈/원정 뱃지)
- 뷰 전환 `SegmentedTabs`: `[달력] [목록]`
  - 목록 뷰: 월 구분선 + GameCard 스트림 (모바일에서 달력보다 훨씬 읽기 쉬움)
  - 달력 뷰: 경기 있는 날에 팀 컬러 도트, 홈/원정 구분
- 지도는 경기 정보 모달 안으로 이동 (항상 노출할 필요 없음)
- 경기장 정보 + 교통 안내는 모달 하단 아코디언

### 5.4 직관 일기 `/diary/*` — 스크랩북 톤

**목록 (`/diary/read`)**
- Masonry 또는 2열 카드 그리드
- DiaryCard: 사진(4:5) 위에 날짜 스탬프, 아래 칩 줄 `승 · 맑음 · 3층 5구역 12열`
- 승/패는 색으로 구분(`win` 그린 / `lose` 중립 그레이 — 레드 금지)
- 필터바: `[전체] [승] [패] [사진있음]` + 시즌 드롭다운
- 빈 상태: "아직 기록이 없어요 · 첫 직관을 기록해보세요" + CTA
- 우하단 FAB(작성하기) — 모바일 하단 탭과 겹치지 않게 `bottom: calc(nav + 16px)`

**작성 (`/diary/create`)**
현재 좌석 선택이 `sectionToggleMenu` → `rowToggleMenu` → `numberToggleMenu` 3단 토글로 분리돼 있습니다.
- **스텝 폼**으로 재구성: `① 경기 선택 → ② 좌석 → ③ 사진·소감`
- 상단 진행 인디케이터, 하단 고정 `다음/저장` 바
- 좌석 선택은 토글 메뉴 대신 **칩 그리드**(구역→열→번호가 같은 화면에서 순차 확장)
- 날씨/동행/승패는 아이콘 칩 단일 선택

### 5.5 방명록 `/guestbooks/*`

- 사진/글 탭 유지하되 `SegmentedTabs`로 통일
- 사진 탭: 정사각 그리드 + 탭 시 라이트박스
- 글 탭: 말풍선 카드 (작성자 아바타 + 닉네임 + 상대 시각)
- 작성 폼은 목록 상단 인라인 또는 바텀시트 (별도 페이지 이동 제거)

### 5.6 이벤트 `/events`

- 현재 `bg-black/75` 위 흰 텍스트 → **의도된 다크 섹션**으로 승격 (ink-900 배경 + 사진 몰입)
- 이벤트 카드: 히어로 이미지 + 제목 + 기간 + 상태 뱃지(`진행중`/`종료`)
- 5th/6th 특별 페이지는 지금의 커스텀 아트워크 톤 유지 — **단, 헤더/푸터는 공통 셸 사용**
- 사진 갤러리는 라이트박스 + 스와이프

### 5.7 아케이드 `/arcade`

- 전면 다크. 캔버스는 세로형 340×600 고정이었고 반응형(`min(340px,100%)` + `aspect-ratio`)으로 이미 수정 완료 — 디자인 시 세로 비율 유지 전제
- 캔버스 아래: `내 최고점수` / `랭킹 TOP 10` 2블록
- 랭킹은 1~3위 강조(메달 컬러) + 내 순위 sticky 행
- 게임 로딩 중 진행률 바 + 브랜드 스피너

### 5.8 로그인 / 가입 / 마이페이지

- 좌측 브랜드 비주얼 + 우측 폼(데스크톱) / 단일 컬럼(모바일)
- 인풋: 44px 높이, `ink-200` 보더, 포커스 시 `brand-500` 링 2px
- 인라인 검증 메시지(현재 별도 모달 사용) — `alertModal` 의존 축소
- 마이페이지: 아바타 + 닉네임 + 내 활동 요약(일기 N · 방명록 N · 최고점수 N)

---

## 6. 모션 · 인터랙션

framer-motion이 이미 설치돼 있으니 활용하되 절제합니다.

```
페이지 전환   : opacity 0→1, y 8px→0, 240ms ease-out
                (현 globals.css의 translateX(100%) 슬라이드는 과함 — 제거)
카드 등장     : stagger 40ms, 최대 6개까지만
탭/필터 전환  : 활성 인디케이터 layoutId 공유 (280ms spring)
모달          : 모바일=바텀시트 상승 / 데스크톱=중앙 스케일 0.96→1
스켈레톤      : shimmer 1.4s linear infinite
호버          : 카드 y -2px + 그림자 soft→raised, 160ms
사진 스트립   : 현 45s 무한 스크롤 유지, 단 prefers-reduced-motion에서 정지
```

**필수**: `@media (prefers-reduced-motion: reduce)`에서 모든 transform 애니메이션 비활성.

---

## 7. 적용 로드맵

| Phase | 내용 | 영향 범위 |
|---|---|---|
| **0. 기반** | 브레이크포인트 표준화, `* !important` 폰트 제거, 토큰을 `tailwind.config.ts` + CSS 변수로 정의 | 전역 (회귀 주의) |
| **1. 셸** | 헤더 리디자인 + 모바일 하단 탭 + 전역 배경 제거 + `PageHeader`/`Surface` 도입 | layout, header, background |
| **2. 프리미티브** | Button / Card / Chip / SegmentedTabs / Skeleton / EmptyState / ErrorState / Modal | shared/ui |
| **3. 홈** | 히어로 + StatCard + 허브 섹션 | home, profile, stats |
| **4. 콘텐츠** | 뉴스 · 스케줄 재구성 | news, schedule |
| **5. 감성 존** | 일기 · 방명록 · 이벤트 스크랩북 톤 | diary, guestbooks, events |
| **6. 아케이드** | 다크 테마 + 반응형 캔버스 + 랭킹 | arcade |
| **7. 마감** | 접근성 감사, reduced-motion, 다크 모드 검토 | 전역 |

Phase 0~2가 끝나면 나머지는 병렬 진행 가능합니다.

---

## 8. Claude Design 전달용 프롬프트 스니펫

아래를 그대로 복사해 쓰세요. 필요한 섹션만 골라 이 문서와 함께 붙이면 됩니다.

> 스니펫 안의 스탯 수치(12.4 PPG 등)는 레이아웃 확인용 **더미 값**입니다. 실제 값은 `/api/stats`에서 옵니다.

### 8.1 전체 시스템 시안

```
농구선수 이소희(BNK 썸 #6) 팬페이지의 디자인 시스템 시안을 만들어줘.

컨셉: "매치 프로그램" — 경기장 프로그램북/스포츠 매거진 톤.
굵은 타이포, 큰 숫자, 명확한 그리드, 넉넉한 여백. 레드는 액센트로만.

아트보드 구성:
1. 컬러 팔레트 (brand 50~700, ink 50~900, 시맨틱) + 대비비 표기
2. 타입 스케일 (display/h1/h2/h3/body/sm/caption/stat/stat-label)
3. 컴포넌트 시트: Button 4종×3사이즈, Card, Chip, SegmentedTabs,
   StatCard, NewsCard, GameCard, Input, Modal, EmptyState, Skeleton
4. 헤더 (데스크톱/모바일) + 모바일 하단 탭바

브랜드 레드 #E11D33, 중립은 따뜻한 회색 계열.
한국어 UI, Pretendard 기준. 모바일 375px / 데스크톱 1280px 두 폭으로.
```

### 8.2 홈 화면

```
팬페이지 홈 화면을 모바일(375px)과 데스크톱(1280px) 두 아트보드로 디자인해줘.

구성 (위→아래):
1. 히어로 — 선수 인물 사진, 하단 그라디언트, 좌하단에 "LEE SOHEE / 이소희 ·
   BNK 썸 / Guard · 170cm", 우측에 등번호 6을 큰 그래픽으로
2. 시즌 스탯 — StatCard 3~4장 (PPG 12.4 / APG 3.8 / 3P% 36.2 / RPG 2.9),
   숫자는 굵고 크게 tabular, 아래 "전체 기록 보기" 펼치기 버튼
3. 다음 경기 카드 — D-day, 날짜/시각, vs 상대팀, 경기장, 홈/원정 뱃지
4. 최신 소식 — 썸네일 뉴스 카드 3개 + "더보기"
5. 팬들의 직관 일기 — 정사각 사진 가로 스크롤

배경은 단색 #F8F6F9, 콘텐츠는 흰 카드. 반투명 오버레이 사용 금지.
데스크톱은 히어로를 좌 사진 / 우 프로필+스탯 2컬럼으로.
```

### 8.3 직관 일기

```
"직관 일기" 목록과 작성 화면을 디자인해줘. (모바일 375px)

톤: 기본 디자인 시스템 위에 스크랩북 감성 — 사진 테두리, 날짜 스탬프,
살짝 기울인 각도, 티켓 스텁 모티프. 과하지 않게.

목록:
- 상단 필터바: [전체][승][패][사진있음] 칩 + 시즌 드롭다운
- 2열 카드 그리드. 카드 = 사진(4:5) + 날짜 스탬프 + 칩 줄
  (승/패, 날씨, "3층 5구역 12열")
- 승은 초록 #0E9F6E, 패는 중립 회색 (레드 금지 — 브랜드색과 충돌)
- 빈 상태 화면도 함께
- 우하단 작성 FAB

작성 (3스텝 폼):
① 경기 선택 ② 좌석 선택 ③ 사진·소감
- 상단 스텝 인디케이터, 하단 고정 다음/저장 바
- 좌석은 구역→열→번호가 순차 확장되는 칩 그리드
- 날씨/동행/승패는 아이콘 칩 단일 선택
```

### 8.4 뉴스

```
뉴스 목록 화면을 모바일/데스크톱으로 디자인해줘.

- PageHeader "뉴스" + 출처 필터칩 [전체][점프볼][루키]
- 최신 기사 1건은 와이드 피처 카드 (16:9 썸네일, 큰 제목)
- 나머지는 단일 스트림 (모바일 1열 / 데스크톱 2열 그리드)
- 카드: 썸네일, 출처 칩, 제목 2줄 클램프, 상대 시각
- 썸네일 없을 때의 브랜드 플레이스홀더도 같이
- 스켈레톤 로딩 상태 포함
```

---

## 9. 접근성 체크리스트

- [ ] 본문 텍스트 대비 4.5:1 이상 (반투명 배경 제거로 자동 해결되는 항목 다수)
- [ ] 터치 타깃 최소 44×44px (현 헤더 `☰` 버튼 미달)
- [ ] 포커스 링: `brand-500` 2px + 2px offset, `:focus-visible` 사용 (현재 `focus:outline-none` 남용)
- [ ] 모달: 포커스 트랩, ESC 닫기, 배경 스크롤 잠금
- [ ] 이미지 `alt` — 장식용은 `alt=""`
- [ ] 스탯 테이블: `<caption>`, `scope="col"`, 가로 스크롤 컨테이너에 `tabindex="0"`
- [ ] `prefers-reduced-motion` 대응
- [ ] 색만으로 정보 전달 금지 (승/패는 색 + 텍스트 라벨 병기)
- [ ] `lang="ko"` 유지, 폰트에 한글 폴백 명시

---

## 10. 기술 부채 — 처리 현황

리디자인 착수 전 정리 작업입니다. **아래 항목은 2026-09-01 완료** (`chore/cleanup-and-node-upgrade`):

- ✅ `src/components/{shared,news,schedule,events}`, `src/hooks` — 미사용 중복 사본 39개 파일 삭제.
  정본은 `src/features/*` 와 `src/shared/*`. 이제 컴포넌트당 파일이 하나뿐이라
  리디자인을 두 번 적용할 일이 없습니다.
- ✅ `tailwind.config.ts`의 `content`에 `src/features/**`, `src/shared/**` 추가.
  누락 상태에서는 해당 경로에 새로 쓴 클래스가 퍼지될 수 있었습니다.
- ✅ Unity 캔버스 반응형 + `globals.css`의 죽은 `#unityContainer` 규칙 제거.
- ✅ Node 20(EOL) → 24 LTS, `.nvmrc` / `engines` 추가.
- ✅ 빌드가 인증 시크릿을 요구하던 문제 해결 (`config/auth.ts` 지연 평가).

**남은 항목:**

- ⬜ `globals.css`의 `.ReactModal__Content { top: 200% !important }` — 모바일 모달 위치
  하드코딩. §4의 공통 `Modal` 도입 시 제거.
- ⬜ `globals.css`의 `* { font-family: ... !important }` — Pretendard 전환과 함께 제거.
- ⬜ 브레이크포인트 표준화 — 기존 `sm`/`md`/`lg`를 두고 표준 규격의 새 이름을 추가한 뒤,
  리디자인된 페이지부터 옮기고 마지막에 옛것을 제거하는 점진 방식으로 진행.
- ⬜ styled-components와 Tailwind 혼용 — 신규 컴포넌트는 Tailwind로 통일.
