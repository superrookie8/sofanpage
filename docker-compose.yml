services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
      - /app/.next
    command: pnpm dev
    env_file:
      - .env.local
    ports:
      - "3000:3000"
